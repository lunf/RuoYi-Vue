# Code generation
gen: 
  # Author
  author: ruoyi
  # The default generated package path "system" needs to be changed to your own module name, such as "system monitor tool"
  packageName: com.ruoyi.system
  # Automatically remove the table prefix, the default is "false"
  autoRemovePre: false
  # Table prefix (the generated class name will not contain the table prefix, multiples are separated by commas)
  tablePrefix: sys_